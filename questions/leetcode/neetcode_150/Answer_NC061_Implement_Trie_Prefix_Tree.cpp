// NeetCode 150 NC061: Implement Trie Prefix Tree - Answer Workspace
// Topic: Tries
// Difficulty: Medium
// Source: https://leetcode.com/problems/implement-trie-prefix-tree/
// Answer workspace file. Implement the function, then add expanded tests.

/*
Description:
    Design a trie supporting insert, exact word search, and prefix search.

Examples:
    - Operations: insert("apple"), search("apple") -> true, search("app") -> false, startsWith("app") -> true
      Explanation: After performing the listed operations, the observed result should be true, search("app") -> false, startsWith("app") -> true.
    - insert("app"), search("app") -> true
      Explanation: This example should produce true.

Implement:
    void SolveImplementTriePrefixTree();
*/

#include "MathTypes.h"
#include <algorithm>
#include <deque>
#include <limits>
#include <map>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <vector>

void SolveImplementTriePrefixTree() {
    // TODO: implement the accepted solution.
}

bool RunTests() {
    // TODO: add expanded LeetCode-style tests.
    return true;
}

RUN_TESTS()
